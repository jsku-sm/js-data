# ... 생략 ...
    # ══════════════════════════════════════════════════════════════
    # TAB 4 — 분석 결과
    # ══════════════════════════════════════════════════════════════
    with tab4:
        st.markdown(...) # Section 04 헤더
        st.subheader("분석 결과")

        # [이동된 부분 시작]
        # 그림 1: 상자 그림
        st.markdown("---")
        st.markdown("#### 종속변수 문항별 점수 분포")
        st.markdown(_img_html(_BOXPLOT_B64, "종속변수 상자 그림"), unsafe_allow_html=True)
        # ... 분포 주요 발견 및 히스토그램 코드 ...
        # [이동된 부분 끝]

        st.markdown("#### 다중공선성 검증 (VIF)")
        # ... 기존 분석 결과 코드 ...
